# Copie para jenkins/.env se quiser trocar o usuario/senha padrao do Jenkins
# local (admin/admin). O docker compose le esse arquivo automaticamente.
JENKINS_ADMIN_USER=admin
JENKINS_ADMIN_PASSWORD=admin
